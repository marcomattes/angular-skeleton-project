import { Injectable } from '@angular/core';

@Injectable()
export class FieldLevelService {
    constructor() {
        throw Error('This constructor needs to be mocked!');
    }

    doSomething(): boolean {
        return false;
    }
}